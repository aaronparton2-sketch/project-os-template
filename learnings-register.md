# Learnings Register

Append-only log of mistakes, pivots, insights, and workflow improvements.

**Rules:**
- Add a row whenever: a mistake, incorrect assumption, pivot, workflow improvement, or tool "gotcha" happens.
- Keep entries short, factual, and action-oriented.
- If a learning changes workflow, also update CLAUDE.md or the relevant command doc.
- Never delete rows — append only.

---

| Date/Time (local) | Type | Issue / Mistake | Learning | Fix / Next time do this | Files / Links |
|-------------------|------|-----------------|----------|------------------------|---------------|
| 2026-03-19 | Gotcha | Google Workspace alias domains can't connect to Instantly | Instantly doesn't support alias domains — it always sends from the primary account. Need secondary domains with their own user accounts. | When setting up outreach domains: always create as secondary domains with real user accounts, never aliases. Costs ~$11 AUD/mo per seat but is the only way that works with cold email tools. | `HOW-plans/2026-03-16-smart-lead-gen-pipeline.md` |
| 2026-03-19 | Insight | Email warmup is domain-specific, not account-specific | Warming up one domain does nothing for other domains on the same Workspace. Each domain builds reputation independently. | Always warm each outreach domain separately. Budget 4 weeks per new domain before cold sending. | Instantly docs |
| 2026-03-19 | Workflow | Built-in email verification saves $76 AUD/mo vs Hunter.io | SMTP mailbox check + confidence scoring based on email source can replace paid verification services | Build verification into n8n Code node. Only send to high/medium confidence emails. Suppress bounces immediately. | `HOW-plans/2026-03-16-smart-lead-gen-pipeline.md` |
| 2026-03-20 | Insight | Google Workspace Business Plus is overkill for outreach mailboxes | Aaron's on annual Plus at $30.90/user/mo — can't downgrade mid-contract, and Google hides cheaper plans in the upgrade UI. Outreach mailboxes just need to send/receive. | Use Zoho Mail Lite (A$24/user/year) for outreach domains. Keep Google Workspace for primary business email only. Set Plus renewal to Starter at contract end (4 Nov 2026). | — |
| 2026-03-20 | Gotcha | Zoho Mail requires IMAP/SMTP to be manually enabled per account | IMAP and SMTP access are OFF by default in Zoho Mail. Instantly connection fails with "IMAP not enabled" error until you toggle it on. | After creating any Zoho mailbox: immediately log in at mail.zoho.com.au → Settings → Mail Accounts → toggle IMAP ON and SMTP ON before connecting to any external service. | — |
| 2026-03-20 | Gotcha | Zoho App Passwords require 2FA enabled first | Can't generate app-specific passwords for Instantly until Multi-Factor Authentication is turned on for the Zoho account. | Enable 2FA before attempting to generate app passwords. Each Zoho user account needs its own 2FA + app password. | — |
| 2026-03-20 | Workflow | Separate Supabase projects for different concerns | Created a dedicated Supabase project for the lead pipeline (`zdaznnifkhdioczrxsae`) separate from the original project. Keeps pipeline data isolated from client/general data. | Use `SUPABASE_PIPELINE_*` env vars for pipeline DB. Original `SUPABASE_*` vars for client projects. Update `.env` comments to make this clear. | `.env` |
| 2026-03-20 | Workflow | n8n API creates workflows inactive by default | The `active` field is read-only on workflow creation via n8n REST API — can't set it during POST. Workflows are always created inactive. | Don't include `active` in the POST body. Test with Manual Trigger in the UI, then activate cron manually. | `scripts/lead-pipeline/create-n8n-workflows.py` |
| 2026-03-20 | Insight | One consolidated n8n workflow > multiple separate workflows | Aaron prefers all pipeline sections (A, B, follow-ups, digest, health) in one workflow with sticky notes, not 5 separate workflows. Easier to manage and see the full picture. | Always consolidate related automation into a single n8n workflow with clear section notes. | n8n "Automated Lead Pipeline" workflow |
| 2026-03-22 | Gotcha | n8n Cloud can't connect to Supabase via direct Postgres (IPv6) | Supabase direct host (`db.xxx.supabase.co`) resolves to IPv6 only. n8n Cloud gives `ENETUNREACH` on IPv6. Must use the Supabase connection pooler instead. Also needs `allowUnauthorizedCerts=true` for SSL. | Use pooler host from Supabase Management API (`GET /v1/projects/{ref}/config/database/pooler`). For this project: `aws-1-ap-southeast-2.pooler.supabase.com:6543`, user `postgres.zdaznnifkhdioczrxsae`. Note: region is `aws-1` not `aws-0` — always check the API. | n8n credential `Mg98U6sOpU23VaJe` |
| 2026-03-21 | Gotcha | Google Maps "no website" doesn't mean no website exists | Many businesses have websites but never added them to their Google Business Profile. Aaron cold called leads marked "no website" and they DID have websites — credibility killer. False positive rate is probably 20-30%. | Must verify website existence BEFORE outreach. 3-layer check: (1) Google search "[name] [suburb]", (2) try common URL patterns via HTTP HEAD, (3) ABN→domain lookup. Reclassify false positives to Pipeline B instead of discarding. | `HOW-plans/2026-03-16-smart-lead-gen-pipeline.md` line 1092 (mentioned but never implemented) |
| 2026-03-21 | Insight | Residential address as lead scoring signal | Businesses registered at residential/suburban postcodes are more likely to be owner-operated, have no marketing support, and convert better. Credit: Aaron's girlfriend's observation. | Added +2 score boost for residential postcodes in both Pipeline A and B scorers. 80/20 approach: 26 known CBD/commercial postcodes defined, everything else defaults to residential. | `HOW-plans/2026-03-21-residential-address-scoring.md`, `scripts/lead-pipeline/postcode-classifier.js` |
| 2026-03-22 | Gotcha | n8n Code nodes can't use $credentials for OpenAI | `$credentials.openAiApi.apiKey` is not available in Code nodes — it's for native OpenAI nodes only. Code nodes need the key injected directly or use HTTP Request nodes instead. | Don't use $credentials in Code nodes. Either inject the key from .env via deployment script, or use a native HTTP Request node for API calls. | `scripts/fix-personalisers.py` |
| 2026-03-22 | Gotcha | n8n Cloud task runner has 60-second TOTAL timeout per Code node | Even in runOnceForEachItem mode, the task runner processes ALL items within one 60-second window. 52 items x 2-3s OpenAI calls = timeout. The mode only changes the code API ($input.item vs $input.all), not the execution budget. | Never put HTTP calls inside Code nodes for batch processing. Split into 3 nodes: Code (build prompt, fast) -> HTTP Request (API call, native, no timeout limit) -> Code (parse response, fast). HTTP Request nodes don't use the task runner. | `scripts/fix-personaliser-timeout.py` |
| 2026-03-22 | Gotcha | n8n HTTP Request node replaces input data with response | When an HTTP Request node runs, the output is the API response only — all input fields (lead data) are lost. Downstream Code nodes can't see the original data via $input. | Use cross-node references: `$('Previous Node Name').item.json` to access data from earlier nodes. The Parse node reads lead data from Build Prompt and OpenAI response from $input. | `scripts/fix-parse-nodes.py` |
| 2026-03-22 | Gotcha | OpenAI API key expired without warning | The old OpenAI key (sk-proj-uI8...TbA) returned 401 even after adding credits. Adding credits doesn't fix a revoked key. The key was also hardcoded in n8n workflow code from a previous fix. | Always test API keys with a simple curl before deploying. When rotating keys: update .env, re-run deployment script, and revoke old key in OpenAI dashboard. | `.env`, `scripts/fix-personalisers.py` |
| 2026-03-22 | Insight | "Digital presence" is not a compelling pitch for tradies | Cold emails mentioning "increasing digital presence" or "online visibility" sound like marketing agency spam. What resonates: "book more jobs", "get more quotes", "look the part online". Emotional, tangible outcomes beat abstract concepts. | Ban "digital presence", "online visibility", "boosting", "increasing" from all email prompts. Use plain English: "customers Google you before they call", "your competitors have websites and you don't". | `scripts/fix-prompts-v3.py` |
| 2026-03-22 | Insight | Social media references need strict filtering | Referencing any random Facebook post in cold outreach sounds creepy or irrelevant. Only reference posts about SPECIFIC COMPLETED JOBS (kitchen renos, installations, etc). Generic posts, ads, holiday greetings must be ignored completely. | Regex filter in Build Prompt: only include social data if post matches job-related patterns (finished, completed, install, happy client, before/after). Everything else gets standard personalisation. | `scripts/fix-prompts-v3.py` |
| 2026-03-22 | Workflow | Supabase Management API blocked by Cloudflare when using Python urllib | urllib requests to api.supabase.com get 403 (error 1010) from Cloudflare. curl works fine from the same machine. | Use subprocess + curl for Supabase Management API calls, not urllib.request. Cloudflare blocks Python's default User-Agent. | `scripts/lead-pipeline/seed-regions.py` |
| 2026-03-22 | Insight | Cheapest Apify Facebook scraper: danek/facebook-pages-posts-ppr | Pay-per-result at $2.99/1000 results. No monthly fee. Same developer as official actor. 50 leads x 5 posts = 250 results = ~$0.75/run. Free $5/mo Apify credits cover ~6 runs. | Use `danek~facebook-pages-posts-ppr` actor, not the official `apify~facebook-posts-scraper` which charges $29+/mo. | `scripts/deploy-social-enrichment.py` |
| 2026-03-23 | Insight | Trade-specific lingo in cold emails is a differentiator | Generic cold outreach sounds like every other agency. Using 1-2 trade terms naturally (e.g., "HWS changeover" for plumbers, "board upgrade" for sparkies) signals insider knowledge and cuts through noise. | Embed a trade lingo dictionary in the OpenAI prompt. Key: keep it to 1-2 terms, never force it. 16 trades covered with generic fallback. Review emails weekly to catch forced-sounding terms. | `scripts/deploy-smart-outreach.py`, `reference/trade-lingo.md` |
| 2026-03-23 | Insight | Social personalisation must be gated on recency + relevance | Referencing a 6-month-old Facebook post or a generic ad in cold outreach is worse than no personalisation at all. Only reference posts about completed jobs, tips, or reviews from the last 60 days. | Social gating decision tree: has Facebook + has post + post within 60 days + matches job/tip/review regex. If any fail, revert to standard outreach. Never reference generic ads, holiday posts, or reshares. | `scripts/deploy-smart-outreach.py` |
| 2026-03-23 | Workflow | Deterministic humour selection is better than random for A/B testing | Random selection clusters humour emails on some days and none on others, making A/B comparison noisy. Deterministic (hash of business name + day) gives exactly ~10% and is reproducible. | Use `(nameHash + dayOfYear) % 10 === 0` instead of `Math.random() < 0.1`. Ensures clean A/B data from day one. | `scripts/deploy-smart-outreach.py` |
| 2026-03-23 | Workflow | ABR SOAP API can be called directly from n8n Code node | No need for the Python script + Execute Command approach. The SOAP envelope is simple XML, and n8n's httpRequest helper handles it fine. Regex XML parsing is ugly but works for the simple ABR response format. | Call ABR directly from Code node instead of shelling out to Python. Saves a dependency (zeep library) and keeps everything in n8n. | `scripts/deploy-multi-source.py` |
| 2026-03-23 | Insight | Instantly webhooks require Hypergrowth plan ($97/mo), not Growth | Aaron's on Growth (~$47/mo). API V2 is available on Growth for polling. Webhooks would be cleaner but not worth the $50/mo upgrade at current volume. | Use API polling (every 6 hours) instead of webhooks for Instantly email event tracking. Revisit when volume justifies the upgrade. | `scripts/deploy-instantly-tracking.py` |
| 2026-03-23 | Workflow | n8n Merge node (Append mode) cleanly combines parallel source branches | When multiple data sources feed into the same pipeline, use an n8n Merge node with mode=append to combine items from 2 inputs. Works reliably for combining Google Maps leads with HiPages+ABR leads before shared scoring. | Use Merge node for parallel source combination. Max 2 inputs per Merge node — chain if needed for 3+. | `scripts/deploy-multi-source.py` |
| 2026-03-24 | Workflow | Chain Merge nodes for 3+ source inputs in n8n | n8n Merge nodes only accept 2 inputs. To combine 3+ sources (Facebook + GMaps/HiPages/ABR), chain two Merge nodes: Merge Pipeline A (GMaps + HiPages/ABR) then Merge All Sources A (Pipeline A result + Facebook). | For N sources needing to merge, use N-1 Merge nodes chained in sequence. Each Merge takes 2 inputs max. | `scripts/deploy-facebook-source.py` |
| 2026-03-24 | Gotcha | Python print with Unicode arrows fails on Windows cp1252 | Print statements containing Unicode arrows or special characters cause `UnicodeEncodeError: 'charmap' codec can't encode character` on Windows with cp1252 encoding. | Use ASCII characters (`->` instead of arrows) in Python print statements. Or set `sys.stdout.reconfigure(encoding='utf-8')` at the top of the script. | `scripts/deploy-facebook-source.py` |
| 2026-03-24 | Insight | Deterministic A/B tests need independent hash functions | Using the same hash (nameHash + dayOfYear) for both humour and offer type creates correlation. If humour fires on index 0, offer type mates_rates also fires on index 0. Use different hash multipliers to decouple. | Humour: `(nameHash * 7 + dayOfYear) % 10 === 0`. Offer type: `(nameHash + dayOfYear) % 10`. Different multipliers ensure independence. | `scripts/deploy-offer-variants.py` |
| 2026-03-27 | Insight | Real client feedback: "I get these messages every day" | Aaron closed a client via text->call->Meet. Client said what stood out was: FREE offer, LOCAL Australian, PERSONAL touch. Generic cold outreach is invisible. | Design all outreach (email + SMS) around: (1) acknowledge you're cold-outreaching, (2) prove you're local, (3) make the offer genuinely irresistible and risk-free. First 10 words make or break it. | `scripts/deploy-pattern-interrupt.py` |
| 2026-03-27 | Workflow | Twilio API needs form-encoded POST, not JSON | Twilio Messages API uses `application/x-www-form-urlencoded` body format. Sending JSON returns an error. n8n HTTP Request node with raw body type + manual URL encoding works. | Use `encodeURIComponent()` for To, From, Body params. Set Content-Type to `application/x-www-form-urlencoded`. Basic Auth with Account SID:Auth Token. | `scripts/deploy-sms-outreach.py` |

---

**Types:** `Mistake` · `Pivot` · `Insight` · `Workflow` · `Gotcha`
